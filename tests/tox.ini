[tox]
setupdir = ..

[testenv]
setenv =
    PYTHONPATH = {toxinidir}/..:{toxinidir}
downloadcache = {toxworkdir}/_download/
distribute = false
commands =
    {envbindir}/coverage erase
    {envbindir}/coverage run --branch --include=*consent* {envbindir}/django-admin.py test --settings=tests.settings
    {envbindir}/coverage report -m


[testenv:sphinx-docs]
changedir = ../docs
deps =
    Sphinx
    django
    versiontools
commands =
    {envbindir}/sphinx-build -W -b html -d {envtmpdir}/doctrees . {envtmpdir}/html
    {envbindir}/sphinx-build -b doctest -d {envtmpdir}/doctrees . {envtmpdir}/doctest


[testenv:django-trunk]
deps =
    coverage
    mock
    south
    svn+http://code.djangoproject.com/svn/django/trunk#egg=django


[testenv:django-1.3.X]
deps =
    coverage
    mock
    south
    django==1.3.1
